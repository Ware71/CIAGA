# Normalize text files to LF in repo (works best with Vercel/Linux)
* text=auto eol=lf

# Windows scripts can remain CRLF if you ever add them
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
